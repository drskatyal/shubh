import { MotionPreviewScreen } from './src/motion';

/** Preview until Home lands. Mount `SkyBackdrop` behind the glance when it does. */
export default function App() {
  return <MotionPreviewScreen />;
}
